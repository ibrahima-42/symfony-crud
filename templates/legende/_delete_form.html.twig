<form method="post" action="{{ path('app_legende_delete', {'id': legende.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ legende.id) }}">
    <button class="btn">Delete</button>
</form>
