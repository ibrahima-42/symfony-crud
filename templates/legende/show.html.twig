{% extends 'base.html.twig' %}

{% block title %}Legende{% endblock %}

{% block body %}
    <h1>Legende</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ legende.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ legende.name }}</td>
            </tr>
            <tr>
                <th>Pseudo</th>
                <td>{{ legende.pseudo }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ legende.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_legende_index') }}">back to list</a>

    <a href="{{ path('app_legende_edit', {'id': legende.id}) }}">edit</a>

    {{ include('legende/_delete_form.html.twig') }}
{% endblock %}
